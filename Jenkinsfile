pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'First build running'
                sh 'ls -la'          // prove we cloned something
            }
        }
    }
}
